// Desktop entry for the example app. `kg build` compiles this file into a native application via `deno desktop`.
// Unlike the page bundle this entry is standalone: it does not serve or embed the `page/` directory. Replace the body
// below with the real desktop UI entry (see https://docs.deno.com/go/desktop).
console.info('KartoffelGames example desktop app.');
